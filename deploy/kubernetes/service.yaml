apiVersion: v1
kind: Service
metadata:
  name: claw-device-pairing
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: claw-device-pairing-tls
spec:
  selector:
    app: claw-device-pairing
  ports:
  - name: oauth-proxy
    port: 443
    targetPort: 8443
